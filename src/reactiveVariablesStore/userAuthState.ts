// import {makeVar} from '@apollo/client';
// import {FirebaseAuthTypes} from '@react-native-firebase/auth';
// import {FirebaseFirestoreTypes} from '@react-native-firebase/firestore';

// type User = FirebaseAuthTypes.UserCredential | null;

// export type AdditionalUserData = {
//   birthDate: FirebaseFirestoreTypes.Timestamp;
//   gender: string;
// };

// export const UserData = makeVar<User>(null);

// export const AdditionalUserData = makeVar<AdditionalUserData | null>(null);
